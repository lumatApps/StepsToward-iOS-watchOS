# ---------------------------
# iOS / Xcode / Swift / SwiftUI
# ---------------------------
# Keep these under version control: *.xcodeproj, *.xcworkspace, shared schemes,
# and Package.resolved (for reproducible builds).

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
.Trashes
._*

# Xcode build + user-specific
DerivedData/
build/
*.xccheckout
*.xcuserstate
*.moved-aside
*.xcscmblueprint

# Xcode workspace/project user data (keep shared schemes)
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Test/coverage artifacts
*.xcresult
*.xccoverage

# Swift Package Manager
.build/
.swiftpm/
!Package.resolved
!**/*.xcworkspace/xcshareddata/swiftpm/Package.resolved

# Playgrounds (keep sources, ignore local timeline/workspace noise)
timeline.xctimeline
playground.xcworkspace

# CocoaPods (optional: comment out if you commit Pods)
Pods/
# Keep lockfile for reproducible installs
!Podfile.lock

# Carthage (optional)
Carthage/Build/
Carthage/Checkouts/
# Keep resolved for reproducibility
!Cartfile.resolved

# Accio (optional)
.accio/
Dependencies/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output/

# Signing & provisioning (never commit secrets)
*.mobileprovision
*.provisionprofile
*.cer
*.p12
*.p8

# Archives / artifacts
*.ipa
*.dSYM
*.dSYM.zip
*.xcarchive

# LSP / tooling caches
.sourcekit-lsp/

# Tuist (optional)
Tuist/Dependencies/
Tuist/Cache/

# IDEs/editors
.vscode/
.idea/
Steps Toward/GoogleService-Info.plist


# Ignore all GoogleService-Info.plist files anywhere
**/GoogleService-Info.plist
